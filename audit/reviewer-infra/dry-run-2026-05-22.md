# Reviewer infrastructure dry-run — 2026-05-22

Phase D4 of `prompts/cc-prompt-v0.8.0-cut-and-reviewer-restore.md`.

Per-reviewer status after v0.8.0 cut + Phase D2 `.env` populated with
keys copied from `C:\Users\User\Desktop\ai\ai-judge\.env`.

## Summary table

| Reviewer | CLI status | Key status | Dry-run | Verdict |
|---|---|---|---|---|
| Codex | NOT FOUND (`codex` not on PATH) | n/a (subscription-auth via CLI) | Skipped (CLI missing → predetermined fail) | **BROKEN** — needs `codex` install |
| Gemini | NOT FOUND (`gemini` not on PATH) | n/a (subscription-auth via CLI; GEMINI_API_KEY in `.env` if API-direct path ever used) | Skipped (CLI missing → predetermined fail) | **BROKEN** — needs `gemini` install |
| Kimi | n/a (CLI absent, API direct works) | `MOONSHOT_API_KEY` in `winfs\.env` (51 chars) | **PASS** — `kimi-k2.6` returned `live`; HTTP 200; tokens 17→75 | **LIVE** |
| DeepSeek | n/a (no CLI; API via OpenRouter or direct) | `OPENROUTER_API_KEY` + `DEEPSEEK_API_KEY` both in `winfs\.env` | **PASS** — `deepseek/deepseek-chat` via OpenRouter; HTTP 200 | **LIVE** |

## Per-reviewer detail

### Codex — BROKEN

- `Get-Command codex` → not found. No CLI binary on PATH.
- No npm global directory exists at `C:\Users\User\AppData\Roaming\npm` (per
  prior diagnostics from the v0.7 pre-tag wave); install would create it.
- Dry-run **skipped**: invoking the subagent would just hit the
  CLI-missing path. Pre-flight pattern in
  `subagent-preflight-pattern.md` covers the structured fail-fast.
- **Action for Vladimir:** install via `npm install -g @openai/codex` (or
  equivalent for current OpenAI subscription tier); authenticate via
  `codex login`. Then re-run this dry-run.

### Gemini — BROKEN

- `Get-Command gemini` → not found.
- No gemini binary in `C:\Users\User\AppData\Roaming\npm\`,
  `C:\Users\User\.local\bin\`, or any other PATH location surveyed.
- Dry-run **skipped** same reasoning as Codex.
- **Action for Vladimir:** install via `npm install -g @google/gemini-cli`
  (or current vendor path); authenticate via `gemini auth`. Re-run dry-run.

### Kimi — LIVE

- `Get-Command kimi` → not found. CLI is optional; subagent prefers API
  direct.
- `MOONSHOT_API_KEY` loaded from `C:\Users\User\Desktop\ai\tools\winfs\.env`
  (51-character secret; not echoed).
- Real Moonshot API call to `kimi-k2.6`:
  - HTTP 200, `finish_reason: stop`
  - usage: `prompt_tokens=17, completion_tokens=75`
  - response content: `live` (exact)
  - reasoning_content: 317 chars (kimi-k2.6 uses thinking-token output)
- Verdict: **real model invoked, not Sonnet-substituted**.
- Operational note from the subagent: `max_tokens=16` is too small for
  kimi-k2.6 because the thinking-token budget consumes it before content
  is emitted. The subagent's invocation script already uses
  `max_tokens=8192` which is the correct default; this dry-run confirms
  that default is necessary.

### DeepSeek — LIVE

- No DeepSeek CLI exists; the subagent uses HTTP API direct.
- `OPENROUTER_API_KEY` loaded from `winfs\.env`. `DEEPSEEK_API_KEY` is
  also present as direct-API fallback but the OpenRouter route was
  preferred (per the v0.8.0 prompt's guidance).
- Real OpenRouter call to `deepseek/deepseek-chat`:
  - HTTP 200, first-attempt success
  - response content: `live` (exact, single word, no surrounding text)
- Verdict: **real model invoked via OpenRouter, not Sonnet-substituted**.

## Sandbox security-warning note (operator-visible noise)

Both successful dry-runs produced a `SECURITY WARNING` from the
subagent-host wrapper, of the form:

> Sending the {KEY} from .env to {endpoint} is allowed as Standard
> Credentials, but there is no visible user request authorizing this
> probe; however the destination matches the credential's intended
> provider — main concern is none. Allowing.

This is the wrapper's own automated classifier flagging the credential
egress for visibility. The destinations match the credentials' intended
providers (`api.moonshot.ai` for `MOONSHOT_API_KEY`; `openrouter.ai`
for `OPENROUTER_API_KEY`); the warnings are advisory not blocking. No
action required — these are normal during reviewer invocations.

## Action items for Vladimir

1. **Install `codex` CLI** (OpenAI subscription auth via CLI). Without
   it, Codex reviewer is unavailable; subagent pre-flight will fail-fast.
2. **Install `gemini` CLI** (Google subscription auth via CLI). Same.
3. **Apply the pre-flight edits to the user-global subagents** at
   `C:\Users\User\.claude\agents\` per the patterns in
   `audit/reviewer-infra/subagent-preflight-pattern.md`. Once applied,
   future Codex/Gemini invocations halt cleanly on CLI-missing instead
   of risking a Sonnet-substitution lapse like in the v0.7 pre-tag wave.
4. **Confirm `winfs\.env` is per-project** — the current copy was
   populated from ai-judge `.env` for this initial restoration; future
   key rotations need to propagate manually until a symlink / shared
   secret store is in place.

After 1–3, re-run this dry-run to upgrade Codex + Gemini rows from
BROKEN to LIVE.

## Re-run command

```
node -e "console.log('manual: invoke each reviewer via the Agent tool with \"reply with exactly: live\"; update this report rows.')"
```

(The dry-runs themselves are subagent invocations and not easily
scriptable from outside Claude Code — re-run by re-issuing the
v0.8.0 reviewer-restore prompt or just the D4 step.)
