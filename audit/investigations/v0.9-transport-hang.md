# v0.9 #3 — MCP transport-hang investigation

**Status: CLOSED (2026-06-10).** A hang was captured live on 2026-06-09/10
with both-side logging on. Verdict: **the stall is inside Claude Desktop, in
the shared dispatch layer above the per-server MCP clients** — the hung call
never reached CD's own per-server MCP logging, let alone the child's stdin.
winfs is exonerated by both-side logs (every request that reached it on the
incident day was answered in 1–222 ms). See **“Captured repro”** below and
the standalone upstream report:
`audit/investigations/upstream-claude-desktop-transport-hang.md`.

**Honest framing (carried from the prompt):** this wave is very unlikely to
produce a winfs *source* fix. Three prior investigations (bug #1 EPERM_ROOT,
bug #2 PATHEXT, P3 audit-IO) each concluded the same — **the winfs impl is not
the cause; the stall lives in the transport layer.** The expected output here
is (a) diagnostic clarity on *which leg* of the transport stalls, and (b) an
upstream bug report for Anthropic if the fault is in Claude Desktop's MCP
transport. Do not judge the wave by "did winfs get patched" — it likely won't.

## Symptom (observed throughout the 2026-05-22 session)

- Intermittent `No result received … after waiting 4 minutes` on winfs tool
  calls from chat-Claude / Claude Desktop (`winfs:write` of large prompts,
  `winfs:read`, `execute_command`).
- Recovery pattern: 2–3 four-minute timeouts on the *same* call, then the next
  call goes through instantly.
- Most reliable trigger this session: a large `winfs:write` (~10 KB payload).

## The two log sources

### A. winfs-side — this wave's instrumentation

Set `WINFS_TRANSPORT_LOG=<path>` in the winfs launch env. winfs then appends
**metadata only** (never bodies):

```
<ISO ts> RECV <id> <method> <bytes>
<ISO ts> SEND <id> <status> <bytes> <duration-ms>
```

- `RECV` = winfs read the request off stdin. `SEND` = winfs finished
  processing and is about to write the response to stdout (logged *before* the
  wire write, so it survives an outbound-pipe stall).
- `duration-ms` = winfs processing time (RECV→SEND) for that id.

### B. Claude Desktop-side — CD's own MCP log

`%APPDATA%\Claude\logs\` (on this machine:
`C:\Users\Expert\AppData\Roaming\Claude\logs\`):

- `mcp-server-winfs.log` — per-server, winfs only.
- `mcp.log` — combined across all MCP servers (filter lines containing `[winfs]`).

Format (per-server file):

```
<ISO ts> [winfs] [info] Message from client: {"jsonrpc":"2.0","id":N,"method":"…",…}
<ISO ts> [winfs] [info] Message from server: {"jsonrpc":"2.0","id":N,"result":…}
```

- "Message from client" = CD handed the request to winfs's stdin (CD-dispatch).
- "Message from server" = CD read the response off winfs's stdout (CD-receive).
- **CD logs full bodies** (request args, response content — file contents,
  command output, paths). That is CD's log, not ours; our `WINFS_TRANSPORT_LOG`
  deliberately logs none of it. When attaching excerpts to an upstream report,
  redact bodies from the CD log.

### Clock note

CD spawns winfs as a local child process — **both logs use the same system
clock**, so the four timestamps below are directly comparable with no
skew correction.

## The 4-point timeline

For any single request `id`, join the two logs and read off four timestamps:

| Point | Source | Meaning |
|-------|--------|---------|
| `t1` | CD `Message from client` ts | CD wrote the request to winfs's stdin |
| `t2` | winfs `RECV <id>` ts | winfs read it off stdin |
| `t3` | winfs `SEND <id>` ts | winfs finished, about to write stdout |
| `t4` | CD `Message from server` ts | CD read the response off winfs's stdout |

Per-leg latency:

```
inbound  = t2 - t1     CD → winfs   (stdin delivery)
process  = t3 - t2     winfs work   (P3: should be fast, < ~250 ms)
outbound = t4 - t3     winfs → CD   (stdout delivery + CD pickup)
```

A ~240 s (4-minute) stall localizes to whichever leg carries the gap.

## Phase C — reproduction protocol (operator: Vladimir / chat-Claude)

1. **Enable winfs logging.** In Claude Desktop's `claude_desktop_config.json`,
   add an `env` entry to the winfs `mcpServers` block:

   ```json
   "winfs": {
     "command": "node",
     "args": ["C:\\Users\\Expert\\Desktop\\AI\\tools\\winfs\\dist\\index.js",
              "--config", "…\\configs\\local.json"],
     "env": { "WINFS_TRANSPORT_LOG": "C:\\Users\\Expert\\Desktop\\AI\\tools\\winfs\\.transport.log" }
   }
   ```

   (Pick any writable path. The file is created on first event.)

2. **Restart Claude Desktop fully** — tray → Exit (not just close the window) →
   relaunch — so winfs is respawned with the new env. Confirm on stderr /
   in `mcp-server-winfs.log` that winfs printed
   `transport logging ON → <path>`.

3. **Trigger the hang** from a chat-Claude session: issue the operation that
   stalls — a large `winfs:write` (~10 KB payload was the most reliable
   trigger). Repeat until a 4-minute timeout fires.

4. **Mark wall-clock time** when chat-Claude reports the 4-minute timeout.

5. **Capture, around that timestamp:** the winfs transport log
   (`WINFS_TRANSPORT_LOG`), and the CD log
   (`%APPDATA%\Claude\logs\mcp-server-winfs.log`). Note the JSON-RPC `id` of the
   hung call if visible (it is the next id after the last successful response).

Then run the correlation tool (Phase D) over the two captured logs.

## Phase D — correlation analysis

Tooling: `scripts/analyze-transport-hang.mjs` (added this wave). Usage:

```
node scripts/analyze-transport-hang.mjs <winfs-transport.log> <CD mcp-server-winfs.log>
```

It joins the two logs by request id and prints, per id, the four timestamps
and the three per-leg latencies, flagging any leg above a threshold (default
5 000 ms). For the hung id, read which leg carries the multi-second gap.

Decision tree (fill in once a hang is captured):

- **No `RECV <id>` in the winfs log** → the request never reached winfs.
  Stall is on the **CD→winfs inbound leg** (CD didn't deliver, or the inbound
  stdin pipe stalled). winfs can do nothing; → upstream report.
- **`RECV <id>` present, no `SEND <id>`** → winfs received but never produced a
  response. This would **contradict P3** (impl is fast). Re-open a winfs-side
  investigation of that specific request.
- **`RECV` + `SEND` present, small `duration-ms` (< ~250 ms), but `t4`
  (CD-receive) is ~240 s after `t3` (winfs SEND)** → winfs did its job fast;
  the stall is on the **winfs→CD outbound leg** (outbound stdout pipe stalled,
  or CD didn't read the response in time). This is the most likely outcome
  given P3. → upstream report; note any size correlation (does it only happen
  above N bytes?).
- **No hang reproduces with logging on** → Heisenbug: the sync-flush logging
  changed timing enough to mask it. That itself is evidence the hang is
  timing-sensitive (consistent with a transport race). Document and stop.

### Findings (captured 2026-06-09/10)

```
hung request id:        never assigned on the wire — would have been id 2/3… of
                        the 23:18:51Z winfs instance; NO trace in either log
method / approx bytes:  tools/call read (~150–200 B request)
t1 CD-dispatch:         ABSENT — no "Message from client" in CD's
                        mcp-server-winfs.log or combined mcp.log
t2 winfs-RECV:          ABSENT — no RECV in WINFS_TRANSPORT_LOG
t3 winfs-SEND:          n/a — request never arrived
t4 CD-receive:          TIMED OUT — chat surfaced "No result received from the
                        Claude Desktop app after waiting 4 minutes"
inbound  (t2-t1):       n/a — t1 itself never happened
process  (t3-t2):       n/a
outbound (t4-t3):       n/a
stalled leg:            ABOVE inbound — CD's shared dispatch layer, before the
                        per-server MCP client (and therefore before the stdin
                        pipe). The decision tree's first branch ("no RECV →
                        inbound leg") under-specified this case: CD's own log
                        disambiguates "CD didn't deliver" vs "stdin pipe
                        stalled" — it's the former. The pipe is exonerated too.
```

## Captured repro (2026-06-09/10, machine `Expert`)

All timestamps UTC (`Z`). Operator-side observations happened the night of
June 9→10 local time (Tallinn, UTC+3).

### Timeline

1. **2026-06-09T19:22:29Z** — CD logs `Server transport closed unexpectedly …
   process exiting early` + `Server disconnected` for winfs; respawn at
   19:23:02. (Earlier disturbance, context only.)
2. **19:23:02 → 23:18:37** — the respawned winfs instance serves ids 2–13
   (read/stat/write/execute_command): **every response in 1–222 ms** per
   `WINFS_TRANSPORT_LOG`.
3. **23:18:48** — intentional shutdown batch: CD stops **winfs, pdf-viewer and
   Desktop Commander together** (operator full-restart of Claude Desktop after
   hangs). Respawn at 23:18:51.
4. **23:18:51–52** — fresh winfs instance: `initialize` answered in **2 ms**,
   `tools/list` (92 812 B response) in **9 ms**.
5. **23:23:34** — one `tools/call` (`write`, 5 901 B request) is delivered and
   answered in **5 ms**. This is the **last message in BOTH logs, ever.**
6. **2026-06-10 (after 23:23Z)** — chat-Claude issues `winfs:read` of the
   transport log (plus retries). Each attempt hangs ~4 minutes, then the
   client surfaces *"No result received from the Claude Desktop app after
   waiting 4 minutes."* **Neither log contains any trace of these calls**:
   zero `RECV` in `WINFS_TRANSPORT_LOG`, zero `Message from client` in
   `mcp-server-winfs.log`, **zero lines dated 2026-06-10 in any CD MCP log at
   all** (`mcp.log` included). CD's MCP client layer never attempted delivery.

> Note: an earlier read of the capture (used in the interim verdict) showed the
> transport log ending at 23:18:52 — that was **stale NTFS metadata** for a
> file held open by the live winfs process. The true tail includes the
> 23:23:34 exchange; the conclusion is unchanged and in fact strengthened —
> the server answered in 5 ms minutes before the wedge.

### Evidence — winfs `WINFS_TRANSPORT_LOG` tail (metadata only, verbatim)

```
2026-06-09T23:09:56.809Z RECV 11 tools/call 154
2026-06-09T23:09:56.823Z SEND 11 ok 401 14
2026-06-09T23:11:46.081Z RECV 12 tools/call 145
2026-06-09T23:11:46.082Z SEND 12 ok 391 1
2026-06-09T23:18:37.417Z RECV 13 tools/call 174
2026-06-09T23:18:37.419Z SEND 13 ok 136 2
2026-06-09T23:18:51.932Z RECV 0 initialize 245
2026-06-09T23:18:51.934Z SEND 0 ok 164 2
2026-06-09T23:18:52.325Z RECV - notifications/initialized 54
2026-06-09T23:18:52.326Z RECV 1 tools/list 58
2026-06-09T23:18:52.335Z SEND 1 ok 92812 9
2026-06-09T23:23:34.468Z RECV 2 tools/call 5901
2026-06-09T23:23:34.473Z SEND 2 ok 229 5
<EOF — nothing further, ever>
```

### Evidence — CD `mcp-server-winfs.log` tail (bodies redacted)

```
2026-06-09T23:18:48.484Z [winfs] [info] Shutting down server...
2026-06-09T23:18:51.545Z [winfs] [info] Initializing server...
2026-06-09T23:18:51.574Z [winfs] [info] Server started and connected successfully
2026-06-09T23:18:51.656Z [winfs] [info] Message from client: {"method":"initialize",…,"id":0}
2026-06-09T23:18:51.934Z [winfs] [info] Message from server: {"id":0,…"serverInfo":{"name":"winfs-mcp","version":"1.0.1"}}
2026-06-09T23:18:52.323Z [winfs] [info] Message from client: {"method":"tools/list",…,"id":1}
2026-06-09T23:18:52.337Z [winfs] [info] Message from server: {"id":1,"result":{"tools":[…]}}
2026-06-09T23:23:34.467Z [winfs] [info] Message from client: {"method":"tools/call","params":{"name":"write",…},"id":2}   [body redacted]
2026-06-09T23:23:34.473Z [winfs] [info] Message from server: {"id":2,"result":{…"bytes_written":5637…}}
<EOF — zero lines dated 2026-06-10 anywhere in CD's MCP logs>
```

### Server-side health across the whole capture window (since 2026-06-02)

- `WINFS_TRANSPORT_LOG` totals: **523 id-bearing requests RECV'd, 523
  responses SENT — zero unanswered, zero transport-level errors.**
- Incident day (06-09): 31 responses, every one in **1–222 ms**.
- The only >5 s durations in the whole week are `execute_command` calls whose
  *child commands* ran that long (e.g. a `Start-Process …agy.exe` invocation,
  60.0 s; a `node _probe…` script, 145.7 s) — both answered `ok`; both
  cross-checked by id+timestamp against CD's own log. Tool runtime, not
  transport stall.
- Corroboration (operator): the hangs hit **winfs, Desktop Commander and
  Claude-in-Chrome simultaneously**; only a full app exit (tray → Exit)
  clears them; first calls after restart succeed.

`scripts/analyze-transport-hang.mjs` had nothing to join for the hung call —
no id exists on either side. The negative result *is* the localization: the
call died **above** every layer either log can see.

## Phase E — conclusion + upstream report

**Conclusion: CD-side dispatch stall, above the per-server MCP client.** A
refinement of the anticipated "CD→winfs inbound stall" branch: the capture
shows the request never even reached CD's own per-server MCP logging, so the
stdin pipe and the stdio transport proper are exonerated along with the
server. The fault lives in Claude Desktop's shared dispatch layer (consistent
with all MCP servers + Claude-in-Chrome wedging simultaneously and only a
full app restart clearing it).

The completed report for filing with Anthropic is the standalone document:
**`audit/investigations/upstream-claude-desktop-transport-hang.md`** —
symptom, environment, both-side evidence, server exoneration, impact, repro
pattern, mitigations, and the concrete ask.

## Phase F — instrumentation is off by default

`WINFS_TRANSPORT_LOG` defaults to **unset** → `createTransportLogger` returns
`null` → the transport is never wrapped → zero overhead and no behavior change.
Documented in `README.md` and `CLAUDE.md` operational notes. The logger ships
so any future recurrence can be captured immediately, but never runs unless
explicitly enabled.
