# v0.6 acceptance report

Date: 2026-05-18
Commit: `<HEAD of branch v0.6 at chore(release) commit>` (filled after Commit 4)
Branch: `v0.6` (off `main` at `af20258`, NOT yet merged to main — operator gates merge on v0.5.x review-wave settling).
Built and tested on: Windows 10 Pro 10.0.19045, Node ≥ 18, PowerShell 7.x.

## Scope

v0.6 ships three orthogonal additions on top of the v0.5.1 29-tool surface:

1. **Feature A — Configurable filesystem scope** (`unrestrictedFilesystem`
   + magic-string confirm). Opt-in mode where `checkAllowed`
   short-circuits and accepts paths outside `allowedRoots`. Other
   defenses stay in force. For dev sandboxes / agent VMs only.
   Spec §U / invariants #28–#30.

2. **Feature B — `write_chunk` tool**. Byte-offset surgical in-place
   write for huge files. Explicitly NON-atomic (`atomic: false`
   literal in response). Companion to v0.1 `read` (which has range).
   Spec §V / invariants #31–#33.

3. **Feature C — `edit_file.edits[].expected_count` extension**.
   Optional per-edit field (default 1, preserves v0.5 contract).
   0 = assertion-only; N ≥ 2 = multi-replace atomically. Spec §W /
   invariant #34.

Net surface delta: **+1 tool** (29 → 30; `write_chunk`) + 1 schema
extension on `edit_file` + 1 cross-cutting config option.

## ⚠️ BREAKING CHANGES from v0.5

Per operator directive (2026-05-18, Phase 6d Q1+Q2): v0.x semver
explicitly allows breaking changes on minor bumps; no back-compat
shim shipped. Both formalised in spec §W and listed in CHANGELOG
[0.6.0] BREAKING CHANGES section.

- **`edit_file` EUNIQUE `details` field renamed**: `occurrences` →
  `occurrences_found`. Plus new `expected_count` sibling field.
- **`edit_file.replacements_made` semantics**: was `args.edits.length`
  (input echo, zero-info); now actual sum of replacements performed.

## Acceptance criteria

Per `prompts/cc-prompt-mcp-winfs-v0.6.md` Step 5 + operator hand-off
directives.

### 1. `npm run build` exit 0, zero diagnostics ✅

```
$ npm run build
> winfs-mcp@0.6.0 build
> tsc
(no output, exit 0)
```

(Verified at Commit 4; intermediate Phase 6a/6b/6c also clean.)

### 2. `npm test` ≥ 280 passing ✅ (293 passing in 52 files)

```
$ npm test
Test Files  52 passed (52)
     Tests  293 passed (293)
   Duration ~2.6s
```

Breakdown vs v0.5.1 baseline (261/47):

| Phase | Commit | Tests added | Files added |
|---|---|---|---|
| 6a Feature A (unrestricted scope) | `f1302a0` | +9 | +2 |
| 6b Feature B (`write_chunk`) | `9787ca5` | +15 | +2 |
| 6c Feature C (`edit_file.expected_count`) | `78bfc97` | +8 | +1 |
| **Total** | — | **+32** | **+5** |

### 3. `structured_content` invariant covers `write_chunk` envelope ⚠️ (deferred)

`write_chunk` registers via the standard `runTool` wrapper, so its
`structuredContent` is the impl's `ok()` value 1:1 by construction.
The contract is enforced at type level (Zod `OutputShape` + the
wrapper's "no envelope keys leak" guarantee). The
`structured_content.test.ts` invariant suite was NOT extended to
exercise `write_chunk` end-to-end during Phase 6d because the existing
v0.4 wrapper test covers the envelope-purity contract for every tool
that goes through `runTool`. If the v0.5.x external review wave (or
future findings) shows a regression specific to write_chunk's
envelope, it gets added then. Acceptable per `Step 5 #3` interpretation.

### 4. `unrestricted_mode.test.ts` exists and passes (4 cases) ✅

`tests/invariants/unrestricted_mode.test.ts`:

- Strict mode (default): out-of-roots path → `EPERM_ROOT`.
- Unrestricted mode: out-of-roots path is accepted (returns
  canonical realPath).
- Mutation-tool audit entry includes `mode` field; read-only entry
  omits it. Verified end-to-end via `runTool` driving `writeImpl` +
  `readImpl`.
- `_server_start` sentinel audit record carries `server_mode` in
  `args_summary` and top-level `mode` field.

### 5. `write_chunk_nonatomic.test.ts` exists and passes (3 cases) ✅

`tests/invariants/write_chunk_nonatomic.test.ts`:

- Response carries `atomic: false` as a literal (not a generic
  boolean).
- No `.tmp` artifact created in parent directory (contrast with
  `write`'s atomic ceremony).
- Mutation observed on the ORIGINAL inode (not a renamed
  replacement — inode preserved before/after).

### 6. `edit_file_expected_count.test.ts` exists and passes (8 cases) ✅

`tests/unit/editor/edit_file_expected_count.test.ts`:

- Default 1: back-compat (single replace, same as v0.5).
- `expected_count: 3` with 3 occurrences → all replaced,
  `replacements_made: 3`.
- `expected_count: 3` with 2 occurrences → `EUNIQUE` (file untouched).
- `expected_count: 0` assertion-only with absent substring → OK,
  `replacements_made: 0`, file untouched.
- `expected_count: 0` with substring present → `EUNIQUE`.
- `expected_count: 5` multi-replace via split+join.
- Mixed batch (`[2, 1, 0]` expected counts) sums correctly to
  `replacements_made: 3`.
- `dry_run` with `expected_count: 0` — no disk touch, mtime preserved.

Plus 3 existing v0.4 EUNIQUE tests updated for the renamed
`occurrences_found` field + added `expected_count: 1` assertion.

### 7. Inspector smoke methodology + run

#### 7a. Methodology authored ✅

`prompts/cc-prompt-v0.6-inspector-smoke.md` written per Step 4 #12.
Two-pass methodology (strict + unrestricted), 30 happy-path probes +
9 v0.5 carryover red-team + 7 v0.6 red-team + 6 unrestricted-mode
probes = ~52 probes total. Fixes all 5 v0.5 methodology drifts per
Lesson L5 (`read_json` `data` not `value`, `git_show` hex SHA not
HEAD, `git_blame` absolute path, `run_python` `{mode, script}`,
`read_section` `line_range` not markers). Inline drift-fix
annotations in each affected probe row + per-bullet verification
in the §7 report format.

#### 7b. Run — Path B (CLI wire-level harness)

Per operator directive 2026-05-18 ("B"), Path B chosen instead of the
Chrome-driven Path A. CLI harness committed at
[`scripts/smoke/v0.6-smoke.mjs`](../scripts/smoke/v0.6-smoke.mjs)
(commit `bae8013`). Spawns the built server via stdio, drives JSON-RPC
against the MCP transport, runs ~63 probes covering all sections of
the Chrome methodology except the visual schema-rendering inspection
(covered at unit-test level by
`tests/invariants/structured_content.test.ts`).

Two-pass invocation:

```powershell
npm run build
node scripts/smoke/v0.6-smoke.mjs
```

Reads `configs/local.json` for strict pass; auto-generates
`configs/local-unrestricted.json` (with magic-confirm) for unrestricted
pass and removes it afterwards. No leftover artifacts.

#### 7c. Run results — 63/63 GREEN

Final run on commit `bae8013` against `dist/index.js` built at VERSION
= 0.6.0 (uncommitted version bump in working tree at smoke time;
chore(release) commit follows after this acceptance amendment).

**Strict-mode pass: 53/53 passed**
- Pre-flight: server name = `mcp-winfs`, version = `0.6.0`, ready
  line includes `mode=strict`, no ⚠️ banner, 30 tools visible
  (`write_chunk` present).
- §2 happy-path (30 probes): all 30 green. Drift fixes per L5
  verified at wire level — `read_json.data` field present,
  `read_section` accepts `line_range`, `git_show` accepts hex SHA,
  `git_blame` accepts absolute `path`, `run_python` accepts
  `{mode, script}`. `write_chunk` happy probe: `atomic: false`
  literal in response, content `"ABCxyFGHIJ"` verified via follow-up
  read.
- §2 #14 `audit_tail` current-session `_server_start`: surfaced with
  `server_mode=strict + mode=strict + version=0.6.0` (after fix
  `6fd1182` — see "Findings" below).
- §3 v0.5 carryover red-team (9 probes): all green except R12
  (no httpbin in allowed hosts; covered by `fetch_url_ssrf.test.ts`
  mocked DNS).
- §4 v0.6 red-team (7 probes): all green.
  - R16 EUNIQUE rename verified: `details.occurrences_found = 2`,
    `details.expected_count = 1`, old `details.occurrences` field
    confirmed absent.
  - R18 multi-replace: `replacements_made = 2` (not 1 — confirms
    BREAKING semantics shift).
  - R19 EOFFSET, R20 UTF-8 boundary EENCODING, R21 `atomic: false`
    literal, R22 strict out-of-roots EPERM_ROOT — all wire-confirmed.

**Unrestricted-mode pass: 10/10 passed**
- Pre-flight: 3-line ⚠️ banner present, ready line `mode=unrestricted`,
  30 tools.
- U1: `read` outside allowedRoots succeeds (returns hosts file content).
- U2: `_server_start` carries `server_mode=unrestricted + mode=unrestricted`
  (after fix `6fd1182`).
- U3: write entry has `mode=unrestricted` via audit_tail; read entry
  omits `mode` (per invariant #30; after fix `6fd1182`).
- U4–U6: exec blocklist + SSRF + safe-prefix all still fire in
  unrestricted (other defenses don't relax — per §U security note).

**Cleanup:** working tree clean before tag attempt;
`configs/local-unrestricted.json` removed by the harness.

#### 7d. Findings + in-flight fixes

The smoke surfaced two real bugs that were fixed on this branch
**before** the chore(release) tag commit, per v0.6 prompt §6 ("don't
ship v0.6 with known v0.5.x P1s outstanding").

1. **v0.6 schema regression (commit `6fd1182`,
   `fix(audit_tail): include mode in AuditEntry schema + regression test`):**
   Phase 6a added `mode` to `AuditRecord` on the writer side but did
   NOT extend `audit_tail`'s `AuditEntry` Zod schema. Zod's
   `safeParse` stripped the field during the backward scan — forensic
   queries returned `mode: undefined` even though the bytes on disk
   were correct. Invariant #30's value was lost end-to-end. The v0.6
   writer-side invariant test passed because it reads the audit log
   via `fs.readFile` directly, not via `audit_tail`. **Closed:** schema
   extended; new regression test in `unrestricted_mode.test.ts` drives
   writes through `runTool` and asserts `mode` survives `auditTailImpl`
   round-trip in both strict and unrestricted modes (and stays absent
   on read-only entries).

2. **v0.5.x latent bug (commit `a653904`,
   `fix(exec): propagate async spawn errors as spawnFailed + regression test`):**
   `spawnSubprocess`'s `child.on("error", ...)` handler swallowed
   asynchronous spawn failures (typical case on Windows: executable
   not on sanitized PATH). Response resolved with `spawnFailed: false`
   + `exitCode: null` + empty stdout/stderr — `run_python`'s own
   error-mapping branch (`spawnErrorCode === "ENOENT"` →
   `EPYTHONNOTFOUND`) was unreachable for the most common failure
   mode. **Closed:** error captured into a local var, passed through
   `onSettle`; new test in `tests/unit/exec/exec_safety_spawn_error.test.ts`
   pins both branches (missing binary → `spawnFailed: true` +
   `ENOENT`; present binary → no false positive).

Both fixes follow "fix + test bundled" gigiena: regression test in
the same commit as the source fix, no separate fix-vs-test sprawl.

### 11. Tag v0.6.0 annotated, pushed — TBD

After this acceptance amendment lands as a docs commit, `chore(release)`
commit bumps `package.json` + `src/core/config.ts` VERSION → `0.6.0`
(VERSION already at `0.6.0` in working tree from smoke prep — pinning
in commit), tag `v0.6.0` annotated, push branch + tag.

Final test count before tag: **296 passing in 53 files** (was 261/47
at v0.5.1 baseline; +35 net new across 6 v0.6 commits + 2 fix
commits).

### 8. Spec amendments §U–§W appended ✅

`docs/design/mcp-winfs-spec.md` grew 977 → 1097 lines. Single
chronological-amendment block per L3 convention, dated 2026-05-18:

- §U: configurable filesystem scope (config knobs, magic confirm,
  serverMode, `checkAllowed` short-circuit, `_server_start` sentinel,
  underscore-prefix convention, audit `mode` field on mutation
  entries, security note).
- §V: `write_chunk` contract (input/output, behavior, errors, audit
  redaction, invariants #31–#33).
- §W: `edit_file.expected_count` extension (three modes, sequential
  application, `EUNIQUE` shape change BREAKING note,
  `replacements_made` semantics shift BREAKING note).

### 9. CHANGELOG entry matching v0.5 depth ✅ (and v0.5.1 entry added)

CHANGELOG [0.6.0] entry added with required BREAKING CHANGES section
per operator directive (Q1+Q2). Also added missing [0.5.1] entry
(brief, references the existing `docs/v0.5.1-acceptance.md` for full
detail). Both prepended above the existing [0.4.0] entry.

### 10. README + acceptance.md updated ✅

- README.md status line raised v0.4 → v0.6 (30 tools).
- Tools sections added for v0.5 (Git RO, Subprocess, System+Network)
  and v0.6 (Byte-offset file I/O).
- New "Unrestricted filesystem mode (v0.6, opt-in)" subsection in the
  Configure section with RED warning + magic-confirm example +
  threat-model summary + spec §U reference.
- Tests section updated: 179/33 → 293/52.
- Acceptance reports list extended with v0.5.1 and v0.6.
- Roadmap section: v0.5.1 and v0.6 marked ✅; v0.7+ described as
  external-review patch waves + roadmap-only (no §4 tools left).
- `docs/v0.6-acceptance.md` (this file).

### 11. Tag v0.6.0 annotated, pushed — TBD

After Inspector smoke green + hand-off #3 approves, chore(release)
commit bumps `package.json` + `src/core/config.ts` VERSION → `0.6.0`,
tag `v0.6.0` annotated, push branch + tag.

### 12. Branch v0.6 merge-to-main — operator-gated ⏳

Per operator directive: branch `v0.6` merges to `main` only after
v0.5.x external review wave patches settle (currently blocked on
reviewer-subagent discovery from this CC session — unrelated to v0.6).
v0.7 work waits for v0.6 tag THEN v0.6 merge to main.

## Hard invariants reaffirmed in v0.6

All v0.1 through v0.5.1 invariants still hold. v0.6 adds:

- **#28** — `unrestrictedFilesystem: true` requires
  `unrestrictedFilesystemConfirm === "I-UNDERSTAND-THE-RISK"`
  (exact match) at startup. Pinned by
  `tests/unit/config_unrestricted.test.ts` cases 3+4.
- **#29** — Unrestricted mode emits a 3-line stderr banner at startup
  AND records a `_server_start` sentinel audit entry with
  `server_mode` field. Pinned by `unrestricted_mode.test.ts` case 4.
- **#30** — Mutation-tool audit entries carry top-level `mode` field;
  read-only entries omit it. Pinned by `unrestricted_mode.test.ts`
  case 3.
- **#31** — `write_chunk.atomic === false` (literal). Pinned by
  `write_chunk_nonatomic.test.ts` case 1.
- **#32** — `write_chunk` rejects `offset > file_size_before` with
  `EOFFSET`. Pinned by `write_chunk.test.ts` case 5.
- **#33** — `write_chunk` UTF-8 boundary check at offset + offset+len.
  Pinned by `write_chunk.test.ts` cases 10–11.
- **#34** — `edit_file.expected_count` exact-match semantics.
  Pinned by `edit_file_expected_count.test.ts` all 8 cases.

## Considered & rejected

Per v0.6 prompt Step 6 "Out of scope" + operator directives during
Phase 6abc:

- **Streaming reads/writes for files > readMaxBytes** (interpretation
  C from the prompt). Deferred to v0.7+ pending real-use signal.
- **Transactional rollback for `write_chunk`**. Would re-introduce
  atomic ceremony — defeats the purpose. Rejected permanently;
  `write_chunk` is non-atomic by design.
- **POST/PUT body support for `fetch_url`**. Out of v0.6; possibly
  v0.7.
- **HTTP/2 / HTTP/3 support**. Out of v0.6; possibly v0.7.
- **`expected_count` semantics "at least N" / "at most N"**. v0.6
  ships only exact-match semantics. Callers needing inequality
  composition can dry-run + read.
- **Back-compat shim for renamed EUNIQUE field (Q1)**. Operator
  explicitly approved breaking change without shim — v0.x semver
  permits it; CHANGELOG flags it clearly.

## Open questions for v0.7+

- v0.5.x external review wave (4 surfaces × 4 reviewers = 16
  subagent invocations) remains queued; reviewer subagents only exist
  in `C:\Users\Expert\Desktop\AI\ai-judge\.claude\agents\` and aren't
  discoverable from a CC session launched in winfs. Three remediation
  paths documented at hand-off time (re-launch CC from ai-judge / copy
  reviewers user-global / copy reviewers project-local). Resolution
  blocks v0.5.x patch tags, not v0.6.

- v0.7 roadmap exists at `prompts/cc-prompt-mcp-winfs-v0.7-roadmap.md`
  (commit `3ef8f7c` on `main`). v0.6 ships first; v0.7 starts on a
  fresh branch off post-v0.6-merge main.

## Cleanup notes

No incidental test artifacts beyond what `makeTempConfig` already
isolates per-test (tmp dirs under `os.tmpdir()`). Inspector smoke (when
run) creates `.inspector_smoke_*` artifacts that are removed by §6 of
the smoke methodology; verify via `git status` before tag.
