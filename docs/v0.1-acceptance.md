# v0.1 acceptance report

Date: 2026-05-16
Commit: `<see git log>` — closure post-v0.1.1 hotfix
Built and tested on: Windows 10 Pro 10.0.19045, Node bundled with this
workspace.

## Acceptance criteria

The criteria are quoted from
[`prompts/cc-prompt-mcp-winfs-v0.1.md`](../prompts/cc-prompt-mcp-winfs-v0.1.md)
§"Acceptance criteria для v0.1".

### 1. Build проходит — `npm install && npm run build` без ошибок ✅

Evidence:

```
$ npm install --no-audit --no-fund
added 141 packages in 12s
$ npm run build
> mcp-winfs@0.1.0 build
> tsc
(no output, exit 0)
```

`tsconfig.json` enables `"strict": true`. No `any` is used outside the
SDK boundary at `tool_wrapper.ts` (cast back to `Result<TValue>` after the
timeout race — explicitly typed, not implicit).

### 2. Unit tests зелёные — `npm test` ✅

Evidence (v0.1.0):

```
$ npm test
Test Files  10 passed (10)
     Tests  44 passed (44)
  Duration  469ms
```

Breakdown:

- `tests/unit/fs/read.test.ts` — 8 tests
- `tests/unit/fs/write.test.ts` — 5 tests
- `tests/unit/fs/append.test.ts` — 4 tests
- `tests/unit/fs/list.test.ts` — 6 tests
- `tests/unit/fs/stat.test.ts` — 4 tests
- `tests/invariants/utf8.test.ts` — 2 tests (write→read roundtrip, BOM strip on read)
- `tests/invariants/allowed_roots.test.ts` — 5 tests (`..`-escape, outside-sandbox, EINVAL, inside-root, symlink-escape with platform-aware skip)
- `tests/invariants/timeouts.test.ts` — 4 tests (hang detection, abort signal, resolveTimeoutMs clamping)
- `tests/invariants/atomic_write.test.ts` — 3 tests (no BOM, no .tmp remnant, append concatenation)
- `tests/invariants/audit.test.ts` — 3 tests (content redaction, string truncation, JSONL writes)

**v0.1.1 hotfix** added `tests/invariants/structured_content.test.ts` (4 tests pinning the schema-validation contract); total post-hotfix: **48 passing**.

### 3. MCP Inspector smoke ✅

Run on 2026-05-16:

```
npx @modelcontextprotocol/inspector node dist/index.js -- --config configs/local.json
```

`--` separator required — without it, Inspector grabs `--config` as its
own flag and tries to load `configs/local.json` as a *list-of-servers*
config, failing with "No servers found". `configs/local.json` is the
gitignored working config; `configs/default.json` ships as a template
with placeholder allowedRoots only.

All 5 tools listed and called successfully through the Inspector UI:

- `stat` on `README.md` → `{exists: true, is_dir: false, size: 5915, mtime, ctime, mode: "00666"}`
- `list` on project root, depth 1 → 20 entries
- `read` on `README.md` → 143 lines, 5915 bytes, `truncated: false`
- `write` `test-cyrillic.txt` with «Тест: Привет, мир.» → 31 bytes, `created: true`
- `append` same file with «\n\nДописали.» → 19 bytes added, new size 50

Re-read of `test-cyrillic.txt` returned exactly `Тест: Привет, мир.\n\nДописали.` — Cyrillic identical, no BOM, line endings preserved.

Negative probe `read` on `C:\Windows\System32\drivers\etc\hosts` →
`EPERM_ROOT` structured error with `isError: true` and `allowedRoots`
echoed in `hint`.

Inspector flagged `Validation Error: data should NOT have additional
properties` on every tool response because `structuredContent` contained
service-envelope fields (`ok`, `tool`, `hint`) outside the declared
`outputSchema`. Filed as `docs/v0.2-backlog.md` #1 and **resolved in
v0.1.1 hotfix** (see `CHANGELOG.md`).

### 4. Real Claude Desktop test ✅ (post-v0.1.1)

Run on 2026-05-16 in MSIX-install Claude Desktop. The v0.1.0 attempt had
Claude misinterpret every successful tool response as `EPERM_ROOT` —
client-side schema validation of the mismatch flagged in §3 was strict
enough to mark each response as an error to the conversational LLM, even
though `isError: false` and the audit log recorded
`result_status: "ok"`. v0.1.1 hotfix (omit `structuredContent` envelope,
return pure payload that matches `outputSchema` 1:1) cleared the issue —
verified by re-running the same probes:

- [x] `read` README.md range `[1, 30]` → 30 lines returned, `truncated: true`
- [x] `list` project root depth 1 → 21 entries (9 dirs, 12 files)
- [x] `stat` `package.json` → all 6 outputSchema fields, mtime/ctime 2 ms apart (atomic rewrite signature)
- [x] `write` `desktop-test.txt` with «Тестовая проба Claude Desktop. Русский язык 🎌» → 72 bytes (UTF-8: 44 visible chars; Latin+space=1, Cyrillic=2, emoji=4), `created: true`, no BOM
- [x] `read` same file → byte-identical Cyrillic+emoji round-trip, no trailing newline

Negative probe `read` on `C:\Windows\System32\drivers\etc\hosts` →
`EPERM_ROOT`, presented to conversational Claude as a clear denial (no
hallucination back to EPERM-on-existing-path that v0.1.0 produced).

Audit log (`%LOCALAPPDATA%\mcp-winfs\audit.jsonl`) cross-confirmed every
successful tool call as `result_status: "ok"` and matched timestamps to
the Claude Desktop chat session.

Two operator notes captured during this run, worth folding into
README troubleshooting:

- MSIX install of Claude Desktop requires the absolute `node.exe` path
  in `claude_desktop_config.json` (`"command": "C:\\Program
  Files\\nodejs\\node.exe"`). The inherited PATH does not resolve `node`
  for spawned child processes under MSIX virtualisation.
- Working config lives in `configs/local.json` (gitignored).
  `configs/default.json` ships as a template with placeholder
  allowedRoots only; copying to local with real paths is the
  recommended dev-loop. Strict Zod config schema rejects unknown keys
  including `_comment` — JSON-native commentary is not supported.

### 5. Allowed-roots работает ✅

Verified by `tests/invariants/allowed_roots.test.ts` and
`tests/unit/fs/read.test.ts > 'returns EPERM_ROOT for a path outside
allowedRoots'`. Probe target `C:\Windows\System32\drivers\etc\hosts`
resolves to a real path that is not under any `allowedRoots` entry; the
result is `EPERM_ROOT` with the `allowedRoots` list echoed in the `hint`.

A **non-existent** path outside the sandbox also returns `EPERM_ROOT`
(not `ENOENT`) — eliminates an existence-leak channel. See the
`security` note in `CHANGELOG.md` and the test
`tests/unit/fs/append.test.ts > 'returns EPERM_ROOT outside allowedRoots'`
(target path `C:\Windows\test.log` does not exist, must still return
`EPERM_ROOT`).

### 6. Timeout работает ✅

`tests/invariants/timeouts.test.ts > 'resolves with ETIMEDOUT when task
exceeds deadline'` runs a 5000 ms simulated hang against a 100 ms
deadline. Result returns inside 1 s with `code: "ETIMEDOUT"`. The signal
is aborted so cooperative cancellation works.

For the spec's "huge file" example, `tests/unit/fs/read.test.ts >
'returns ETOOLARGE when file size exceeds max_bytes'` covers the size-cap
path (which fires before the timeout for any realistic case).

### 7. UTF-8 round-trip ✅

`tests/invariants/utf8.test.ts > 'write→read roundtrips Russian + emoji +
ASCII identically'` writes
`Привет, мир\n🚀 emoji line\nplain ASCII\nспецсимволы: ёщъыЁ\n`,
re-reads it, asserts byte-identical, and asserts the written file does
not start with the UTF-8 BOM (`0xEF 0xBB 0xBF`).

End-to-end confirmation through Claude Desktop chat surface in §4 above
(72-byte file with Cyrillic + emoji 🎌, round-tripped to display layer
without corruption).

### 8. Realpath escape blocked ✅

`tests/invariants/allowed_roots.test.ts > 'symlink/junction escape
outside allowed root is blocked'` creates a real symlink inside the
allowed root that points to a directory **outside** any allowed root,
then attempts to read through it. Result: `EPERM_ROOT`.

(On Windows the symlink creation requires Developer Mode or admin
elevation; the test skips itself with platform detection if creation
returns `EPERM`/`EACCES`, so CI on a non-elevated runner still passes
without lying.)

The `..`-escape variant is covered by the same file's first test.

### 9. Audit log пишется ✅

`tests/invariants/audit.test.ts > 'writes JSONL records to the configured
audit path'` invokes `appendAudit` twice (one `ok`, one `error`) and
asserts both records appear as valid JSONL at
`config.resolvedAuditLogPath`. The `'redacts the content field'` test
asserts spec §2.11's redaction contract (content body → `<redacted: N
bytes>`).

Live audit log at `%LOCALAPPDATA%\mcp-winfs\audit.jsonl` provided
end-to-end cross-confirmation of the §3 and §4 manual probes.

## Quality checklist (from `node_mcp_server.md`)

The reference's checklist is reproduced below with v0.1 status. Items
N/A to a v0.1 slice (resources, HTTP transport, pagination at scale)
are marked accordingly.

### Strategic Design

- [x] Tools enable complete workflows — read/write/append/list/stat is the
      minimum useful FS surface for an architect agent.
- [x] Tool names reflect natural task subdivisions.
- [x] Response formats optimise for agent context efficiency —
      `structuredContent` + short JSON in `content[]`.
- [x] Human-readable identifiers used where appropriate (ISO mtimes,
      octal mode strings).
- [x] Error messages guide agents toward correct usage — `hint` field on
      `EPERM_ROOT` echoes `allowedRoots`, `EEXIST` suggests
      `overwrite=true`, `ETOOLARGE` shows the current limit.

### Implementation Quality

- [x] FOCUSED IMPLEMENTATION — v0.1 scope frozen to 5 tools, no
      pre-emptive v0.2+ code.
- [x] All tools registered via `registerTool` with complete configuration.
- [x] All tools include `title`, `description`, `inputSchema`, and
      `annotations`.
- [x] Annotations correctly set (read/list/stat are
      `readOnlyHint: true, idempotentHint: true`; write/append carry the
      correct `destructiveHint`).
- [x] All tools use Zod schemas for runtime input validation. `.strict()`
      is applied at `InputSchema` even though the SDK consumes the
      `InputShape` form — `InputSchema.parse()` is available for tests
      and any direct callers.
- [x] All Zod schemas have constraints and descriptive `.describe()`
      strings.
- [x] All tools have comprehensive descriptions with explicit input/output
      contracts and error catalogue per tool.
- [x] Error messages are clear, actionable, and educational.

### TypeScript Quality

- [x] Interfaces defined for all tool result types.
- [x] `"strict": true` in `tsconfig.json` with extra
      `"noImplicitOverride"` and `"noFallthroughCasesInSwitch"`.
- [x] No untagged `any`. One explicit widening cast remains in
      `tool_wrapper.ts` after the timeout race — flagged inline.
- [x] All async functions have explicit `Promise<T>` return types.
- [x] Error handling uses proper type guards (`NodeJS.ErrnoException` cast
      pattern in `fromNodeError`).

### Advanced Features

- [ ] Resources — N/A for v0.1. Spec §4 has no resource-based tools.
- [x] Transport — stdio configured per spec §1.
- [ ] Notifications — N/A. Tools list is static at v0.1.

### Project Configuration

- [x] `package.json` includes all dependencies.
- [x] Build script produces `dist/index.js`.
- [x] Main entry `dist/index.js`.
- [x] Server name `mcp-winfs`. (Spec uses an unprefixed name because
      this is a single-purpose user-facing server, not a service
      integration.)
- [x] `tsconfig.json` in strict mode.

### Code Quality

- [ ] Pagination — N/A. `list` enforces depth ≤ 5 but does not offset.
      Added in v0.3 alongside `grep`.
- [x] Large responses — `read` honours `readMaxBytes` and reports
      `truncated:true`. `list` is depth-capped.
- [ ] Filtering for large result sets — `list` accepts `glob`. v0.3
      adds `grep` filtering.
- [x] Timeouts on all network/I/O — every tool runs inside `runTool` ->
      `withTimeout`.
- [x] Common functionality extracted — `runTool`, `checkAllowed`,
      `encodeUtf8NoBom`, `atomicWriteFile` are reused across tools.
- [x] Return types consistent (`Result<T>` for impls, `ToolResponse` for
      handlers).

### Testing and Build

- [x] `npm run build` exit 0.
- [x] `dist/index.js` created.
- [x] Server boots (see §3 above).
- [x] All imports resolve.
- [x] Sample tool calls work — 48 unit + invariant tests post-v0.1.1.

## Open items for v0.2 entry

- README troubleshooting section: MSIX `node.exe` absolute path
  requirement + `configs/local.json` dev-loop pattern (both surfaced
  during §4 manual run).
- Evals XML has 2 placeholder questions. Real questions accumulate as
  new tool families land (v0.3+).
