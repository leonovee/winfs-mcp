node_modules/
dist/
coverage/
.vitest-cache/
*.log
.DS_Store

# Local Claude Code state (per-machine)
.claude/settings.local.json

# Local working config with real paths (not the template)
configs/local.json

# Smoke test artifacts (Inspector probes — recreatable, harmless if absent)
test-cyrillic.txt
desktop-test.txt
test-inspector/
archived.txt
archived.backup.txt

# OS / editor
Thumbs.db
.vscode/
.idea/
