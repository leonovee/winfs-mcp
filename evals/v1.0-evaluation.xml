<?xml version="1.0" encoding="UTF-8"?>
<!--
  v1.0 evaluation suite for mcp-winfs.

  Spec §Amendments/2026-05-16/C constraints:
    - Independent (no cross-question state)
    - Read-only (no write/append/mkdir/edit/exec)
    - Complex (≥2 tool calls, real exploration)
    - Realistic (architect-level tasks, not synthetic)
    - Verifiable (single clear answer, string-compare)
    - Stable (no mtime / branch-state dependence)

  v0.1 ships 1-2 example questions as a placeholder. Real questions land
  alongside the tools they exercise:
    - v0.3: grep + read_json questions (search/read combinations)
    - v0.4: edit_file dry-run + read_section questions
    - v0.5: git_log / git_diff / git_blame questions
    - v0.7: full 10-question suite finalised
  Post-1.0 backlog (NOT a v1.0.0 release gate): finalise the 10-question suite +
  run.py runner and hit >=8/10 through MCP. See evals/README.md and spec §10.
-->
<evaluation>
  <qa_pair>
    <!-- v0.1 placeholder. Exercises stat + read + range. Replace with a real
         question against the user's project once handoff content lands. -->
    <question>List the first 5 lines of docs/design/mcp-winfs-spec.md inside the allowed root. What is the literal text of line 1 (excluding the leading "# ")?</question>
    <answer>MCP-WinFS — Спецификация v1.0</answer>
  </qa_pair>
  <qa_pair>
    <!-- Exercises list with depth + glob filtering. -->
    <question>Inside the allowed root, list all `.md` files with max_depth=2. How many distinct files are there?</question>
    <answer>2</answer>
  </qa_pair>
  <!-- TODO v0.3..v0.7: append 8 more questions to reach 10 total. -->
</evaluation>
